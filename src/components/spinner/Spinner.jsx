import React from 'react';
import './spinner.scss';

const Spinner = () => {
  return (
    <div className='spinner-overlay'>
      <span className='spinner' />
    </div>
  );
};

export default Spinner;
